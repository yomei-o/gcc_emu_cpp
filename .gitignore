# Where the toolchain is assembled from Alpine packages.  guest/tree is the
# result and is committed; this is the workshop.
build/
x86emu
x86emu.exe
*.o

# Output from running the examples while testing, which lands wherever the
# emulator was started from.
/*.csv
/a.out
